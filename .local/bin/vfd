#!/bin/bash

dir=$(find ~/work ~/projects ~/sandbox ~/ -type d -print 2>/dev/null | fzf +m) || {
	echo "No directories found"
	exit 1
}
if [ -n "$dir" ]; then cd "$dir"; else
	echo "No selection made"
	exit 1
fi

nvim .
